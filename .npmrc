save-exact=false
fund=false
audit=false

# GitHub release downloads are unreliable from some networks; use a mirror for
# the Electron and electron-builder binaries.
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
